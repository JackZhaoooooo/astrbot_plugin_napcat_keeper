{
    "napcat_url": {
        "description": "NapCat 地址",
        "type": "string",
        "hint": "默认: http://localhost:6099",
        "default": "http://localhost:6099"
    },
    "napcat_token": {
        "description": "NapCat WebUI Token",
        "type": "string",
        "hint": "可选；优先使用该 token 进行 WebUI 鉴权。留空时会尝试从本地 webui.json 自动读取",
        "default": ""
    },
    "webui_config_path": {
        "description": "本地 WebUI 配置路径",
        "type": "string",
        "hint": "仅在未填写 napcat_token 时使用，默认: /root/AstrBot/napcat/config/webui.json",
        "default": "/root/AstrBot/napcat/config/webui.json"
    },
    "check_interval": {
        "description": "检查间隔（秒）",
        "type": "int",
        "hint": "最小 5 秒，默认 30 秒",
        "default": 30
    },
    "request_timeout_seconds": {
        "description": "请求超时（秒）",
        "type": "int",
        "hint": "最小 3 秒，默认 10 秒",
        "default": 10
    },
    "notify_on_initial_logged_out": {
        "description": "启动时若已离线是否通知",
        "type": "bool",
        "hint": "开启后：插件启动后首次检测到未登录也会推送一次通知",
        "default": true
    },
    "notify_retry_cooldown_seconds": {
        "description": "离线通知失败重试间隔（秒）",
        "type": "int",
        "hint": "通知发送失败时会按该间隔重试，最小 5 秒，默认 30 秒",
        "default": 30
    },
    "notify_umos": {
        "description": "退出登录通知 UMO 列表",
        "type": "list",
        "hint": "可填写多个 UMO，检测到退出登录时逐个通知",
        "items": {
            "type": "string"
        },
        "default": []
    },
    "debug": {
        "description": "输出全部轮询日志",
        "type": "bool",
        "hint": "关闭后仅在状态变化、告警和异常时写日志",
        "default": false
    }
}
